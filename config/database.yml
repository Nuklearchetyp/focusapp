# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: focusadmin
  password: ayylmao
  host: localhost
  socket: MySQL

development:
  <<: *default
  database: focusgroup_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: focusgroup_test

production:
  <<: *default
  database: focusgroup_production
  username: focusgroup
  password: <%= ENV['FOCUSAPP_DATABASE_PASSWORD'] %>
